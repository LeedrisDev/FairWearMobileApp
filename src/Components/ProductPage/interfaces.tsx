import {IconName} from "@fortawesome/fontawesome-svg-core";
import fontawesome, {IconDefinition} from "@fortawesome/fontawesome";

export interface ITextandLogo {
    icon: IconDefinition,
    text: string,
}